// const app = getApp();
// import { SendClickLog } from '../../utils/maidian.js';

module.exports = {
    openGoodDetail() {
        // const app = getApp();
        // let { appid, userid } = app.globalData.extConfig;
        // SendClickLog(appid, userid, '{}', 'jz_xcx_good_goodDetail');
    },
};
