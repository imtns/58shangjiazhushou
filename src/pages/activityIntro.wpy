<template>
    <web-view src="{{url}}"></web-view>
</template>
<script>
import wepy from 'wepy';
import Mixin from '../mixin';
import { getUid, SendClickLog } from '../utils/maidian';

export default class ActivityIntro extends wepy.page {
    mixins = [Mixin]
    config = {
        navigationBarTitleText: '活动说明',
    }
    data = {
        url: '',
    }
    onLoad() {
        SendClickLog('wxf03e52adc4b13448', getUid(), '{}', 'sjzh_click_activityIntro_onload');
        const v = (+new Date() * 1).toString(36);
        this.url = `https://yaofa.58.com/yunying/activeIntro.html?v=${v}&from=shangjiazhushou`;
        this.$apply();
    }
}
</script>
<style lang="scss">
</style>
<!--
    此页面用于文章添加选分组，入口从编辑页面进来
-->